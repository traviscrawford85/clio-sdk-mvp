description: NoteCreateRequestDataMatter
properties:
  id:
    description: The unique identifier for a single Matter associated with the Note.
      The keyword null is not valid for this field. Required only if the Note type
      is `"Matter"`.
    title: Id
    type: integer
required:
- id
title: NoteCreateRequestDataMatter
type: object
