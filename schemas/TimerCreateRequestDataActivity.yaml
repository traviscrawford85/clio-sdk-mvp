description: TimerCreateRequestDataActivity
properties:
  id:
    description: The unique identifier for a single Activity associated with the Timer.
      The keyword `null` is not valid for this field.
    title: Id
    type: integer
required:
- id
title: TimerCreateRequestDataActivity
type: object
