description: ActivityCreateRequestDataUser
properties:
  id:
    anyOf:
    - type: integer
    - type: 'null'
    default: null
    description: 'The unique identifier for a single User associated with the Activity.
      Use the keyword `null` to specify no association. On creation, if no user is
      specified, it will default to the current user.  If a TimeEntry is created by
      a Clio Connect user, the field is not editable. '
    title: Id
title: ActivityCreateRequestDataUser
type: object
